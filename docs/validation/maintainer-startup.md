# Maintainer startup and recovery

This checks the repository contributor entry, architect charter and GitHub work convention. Installed plugin behavior is unchanged. Detailed traces and trial artifacts remain in local maintainer evidence.

## Observed coverage

Fresh sessions used the SDK bundled with GitHub Copilot desktop 1.1.14, standalone CLI 1.0.83 and Sol at low reasoning. Repository configuration and instruction discovery were enabled in disposable checkouts. No Atlas profile was selected and no startup file contents were inserted into the user prompts.

- Native instruction-source reporting identified root `AGENTS.md` and `.github/copilot-instructions.md`. Tool records show reads of the charter, contributor workflow and shared lead.
- An architectural recommendation preserved the portable lead, challenged a proposed always-on fixed team and autonomous backlog selection, and kept the ordinary acknowledgment within advisory scope. Both replies supplied next-action guidance.
- With a working read-only GitHub tool, a fresh session read the actual assigned issue, recovered the outcome and unfinished setup, distinguished drafted repository content from completion, and disclosed unavailable Git/PR evidence. Its follow-up rejected treating a board's Done status as delivery proof or authority to start another task.
- An initial harness permission error prevented issue reading. The session reported the access gap and withheld a completion claim. That trial does not establish successful issue recovery; the corrected tool was exercised separately.

All observed worktree snapshots remained unchanged, with local editing available. No live external writes or worker dispatch tools were exposed, so restraint with those capabilities was not tested. The isolated checkouts contained candidate files without full Git history; Git freshness was consequently reported as unavailable rather than proved.

A separate reader, without the design conversation, recovered the intended product, architect mandate and authority distinctions from the repository and inspected the historical summary against existing PR/evidence records. Independent source review found no blocking design or authority defect. The existing 17 evaluator tests pass; they check evaluator correctness, not maintainer judgment.

## Limits and repeat use

These are small native observations, not a reliability estimate or comparison showing improvement. The result supports repository startup in the tested SDK configuration; it does not verify the desktop UI, Windows workplace setup, Claude, saved ChatGPT project configuration or repeated compaction. Instruction availability and successful examples do not guarantee future adherence.

For a changed startup path, use a disposable checkout outside personal instruction scope with the host's repository discovery enabled. Give an ordinary architecture or assigned-issue request without naming the startup files. Inspect actual instruction discovery, source reads, resulting judgment and ordinary follow-ups. Keep the review criteria outside the executing workspace. Exercise unavailable required state separately, preserve scope observations and disclose missing capabilities. Check saved project configuration independently when the host starts outside a checkout.
