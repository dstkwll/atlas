[pytest]
markers =
    integration: real external calls (detached hermes, network); excluded from default dev runs. Run the nightly lane with: pytest -m integration
# Default dev runs exclude the integration lane.
addopts = -m "not integration"
