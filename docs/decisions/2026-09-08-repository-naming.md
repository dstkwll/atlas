# Atlas repository identity

Dan authorized reclaiming the Atlas name for the lightweight successor and preserving the original product privately. This changes repository naming and installation references; it does not import the legacy architecture into the current product.

| Product | Previous repository | Current repository | Visibility |
| --- | --- | --- | --- |
| Current Atlas | `dstkwll/atlas-successor` | `dstkwll/atlas` | Public |
| Original pipeline product | `dstkwll/atlas` | `dstkwll/legacy-Atlas` | Private |

The current product retains GitHub repository ID `1356247587`; the legacy product retains ID `1332926254`. Reusing `dstkwll/atlas` replaces the redirect that would otherwise lead to the renamed legacy repository. No repository or Git history was deleted. Historical links using the original `dstkwll/atlas` name must be interpreted as legacy references; the old successor URL redirects to the current product.

The current package remains named `atlas`; its marketplace becomes `atlas`, making the installation identifier `atlas@atlas`. Existing marketplace installations must explicitly migrate. Work installations were already detached according to Dan. He authorized replacement or removal of the Mac mini personal Atlas skill; this assignment does not authorize changes to Hermes or workplace systems.

Root Drive coordination files predating this decision describe the original repository under its previous name. Read them for coordination procedure, and use the identity mapping here to distinguish product authority. The old architecture is preserved in `legacy-Atlas`; it is not the architecture of this repository. Historical source documents are not rewritten by this migration.
