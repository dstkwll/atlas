# Current work

The active assignment is three optional artifact skills (PRD, vertical-slice tickets, handoff), backed by shared Atlas runbooks, with selective donor adaptations and proportionate TDD guidance. Implementation and a draft PR are authorized; merge and installed-copy updates are separate actions.

The branch is `feat/planning-deliverables`, based on merged main. Canonical behavior stays in `plugins/atlas/skills/atlas/`; the three sibling entry points depend on it. No new runtime, default tracker publication or implementation authority is introduced. Full-package manual installs must retain all siblings; the main skill alone can select every runbook naturally.

Independent forward artifact checks and Copilot textual review completed without a blocking finding. Skill formats, relative links, manifests, whitespace and isolated native CLI discovery passed. See `docs/validation/planning-deliverables.md` for limits. The draft also honors configured topic/vault routing and supports a single HTML PRD with embedded recovery context and a Fluent 2–informed starter. An HTML-only reconstruction and local desktop/mobile/print checks passed within documented limits. Next: review the updated draft before merge; recheck live Git/PR state before acting.
