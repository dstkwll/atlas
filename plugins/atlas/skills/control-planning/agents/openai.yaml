interface:
  display_name: "Atlas Control Planning"
  short_description: "Apply configured System Design authority once"
  default_prompt: "Use $control-planning to apply the configured HUMAN, AGENT_REVIEW, or HUMAN_IF_CHANGED System Design boundary once in the workflow-internal handoff."
policy:
  allow_implicit_invocation: false
