interface:
  display_name: "Atlas Control Planning"
  short_description: "Apply configured System or Program Design authority once"
  default_prompt: "Use $control-planning to apply the configured HUMAN, AGENT_REVIEW, or HUMAN_IF_CHANGED System Design boundary once, or the configured HUMAN or AGENT_REVIEW explicit system_design or program_design boundary once."
policy:
  allow_implicit_invocation: false
