# Architecture and coordination methods

The focused synthetic checks cover selective method use and the quality of the resulting decisions. Raw task histories and host diagnostics remain in local maintainer evidence.

| Coverage | Observed result |
| --- | --- |
| Architecture recommendation | Native Codex skill and Copilot profile sessions read the architecture method, inspected both callers, preserved preparation versus approved commitment, and wrote advisory notes without implementation. |
| Contributions with passing isolated checks | Codex read the coordination method, executed the component checks and actual composition, detected the incompatible field and absent unclaimed behavior, and recommended bounded correction without changing source or tests. |
| Planning around unavailable hardware | Sessions read the coordination and verification methods, wrote assignments and integration responsibilities, retained unresolved policy and physical evidence, and did not dispatch or implement after agreement. |
| Small-task controls | The sentence correction returned only the correction; the ordinary shipping repair demonstrated a failing regression check followed by a passing fix without an architecture or coordination exercise. |

Matching merged-guidance runs also produced sound architecture recommendations, found the integration defect and completed the ordinary repair. These small samples support useful application of the new methods; they do not establish a reliability improvement over the baseline.

Dependency planning needs judgment. The fixture requires agreement on a revised result contract and supplies no settled revision, so it cannot establish whether waiting for that contract over-blocks implementation. Available contract preparation remained distinguishable from later execution. The method explicitly asks which output requires a missing decision, but these runs do not prove reliable separation of a settled sub-contract from an unrelated open policy.

Copilot was checked through native profile selection followed by ordinary conversation in a temporary project outside maintainer instruction scope. The planning follow-up preserved its authority boundary but omitted explicit next-action guidance. Its architecture note also retained an aggregate-total price check that can miss offsetting individual price changes; the Codex candidate found this defect. Copilot therefore demonstrated routing and caller-boundary reasoning, with incomplete architectural correctness. Reading a guide and succeeding on one task do not guarantee adherence across turns or compaction. The Copilot checks had local reading and planning writes, without execution or delegation tools; they do not prove dispatch restraint with usable workers, live worker integration, hardware correctness or workplace compatibility. Integration execution evidence comes from Codex using supplied synthetic contributions.

The evaluator's 17 fast tests passed. Changed-guide links and whitespace checks passed. Independent source and evidence review informed the final assessment; these checks do not substitute for the behavioral observations above.
