# Current work

Completed the user-authorized repository-only behavior evaluation pilot on `feat/behavior-eval-pilot`, based on main `8c16b2c`. The user subsequently authorized adding automatic fast evaluator tests to the draft PR and documenting optional local behavioral evidence. Implementation and draft publication are authorized; merge, installed-copy changes and credentialed model CI are excluded. The completed pilot goal does not authorize automatic product-guidance changes.

Canonical Atlas guidance remains `plugins/atlas/skills/atlas/SKILL.md`; product files are unchanged. The reusable runner, fixtures, objective checks and separate review rubric live under `evals/`. Sixteen scheduled native trials, three tool-observability diagnostics and one short-acknowledgment variant completed. Fifteen focused evaluator tests and a final native smoke check pass. Startup and deliberate failure exercises are separate from that schedule. Independent findings were reconciled with source, artifacts and tests.

See `docs/validation/behavior-evals.md` for findings and evidence limits. Correct task outcomes do not establish reference consultation; UI events omit some nested tool details. The runner now retains filtered native tool records and parsing status. A continuation diagnostic supports a consultation gap, compaction preserved task scope without proving full guidance restoration, and a generated ticket used an unsupported accepted-policy label. These observations are not a reliability rate or workplace-host proof.

Detailed synthetic evidence and independent reviews are preserved outside the repository in the sibling workspace `research/2026-09-10-eval-pilot/`. Drive root controls were refreshed on 2026-09-10 and unchanged; the previously inspected Codex inbox had no active dated handoffs. The Evaluator correctness workflow runs the 15 fast tests on PRs and main pushes, without model credentials or inference. All 15 tests pass locally and in GitHub Actions run `34465171431` against `95c6dd8`. Next: review the draft and its evidence. A narrow routing improvement is a possible separate assignment; do not change installed copies or merge without applicable authorization.
