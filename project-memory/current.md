# Current work

The active assignment combines explicit runbook selection with a lightweight direction check in Atlas's lead behavior: revisit the agreed outcome and design roughly every four substantive exchanges, between meaningful autonomous work chunks, and sooner on drift signals. Implementation and a draft PR are authorized; merge and installed-copy updates are separate actions.

The branch is `fix/direction-checks`, based on merged main. The change uses the existing lead, delivery and continuity guidance, with no new skill, runtime, counter, retry bound or mandatory artifact. Preserve ordinary authorized continuation and return for judgment only when accepted commitments need to change.

Independent text-only scenario checks covered drift, aligned continuation, autonomous chunks, trivial retries, missing context and small-task completion. Primary inspection found no blocking textual defect; skill format, links, manifests and whitespace checks passed. See `docs/validation/direction-checks.md` for evidence and limits. The authorized extension makes reading and applying a matching guide explicit before substantive work, exposes common specialist triggers, and checks the original outcome before recovery expands into supporting machinery. Fresh baseline/candidate agents both selected and applied relevant guides from raw tasks; the candidate bypassed the specialist index for failure handling. Small-task behavior stayed proportional. This does not establish an improvement rate or native host adherence; see the validation record. Next: update and review the draft before merge; recheck live Git/PR state before acting.
