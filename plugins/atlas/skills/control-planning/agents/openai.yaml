interface:
  display_name: "Atlas Control Planning"
  short_description: "Apply configured System, Program, or ticket authority once"
  default_prompt: "Use $control-planning to apply one explicit configured System Design, Program Design, or ticket-graph boundary and record at most one transition."
policy:
  allow_implicit_invocation: false
