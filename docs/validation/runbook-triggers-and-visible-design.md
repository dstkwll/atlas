# Runbook triggers and visible design choices

## Change and retained behavior

The routing audit found no orphaned reference among the 36 guides. It did find a specialist gateway framed more narrowly than its library, and index rows that omitted valid entry conditions. The correction includes reported slowness before measurement, prospective model/API changes, requested security review, cleanup and documentation impact. The existing requirement to reconsider guidance when activity or uncertainty changes remains; examples clarify its application rather than introducing a controller.

Discovery now explicitly presents credible competing approaches with shared context, meaningful tradeoffs, a recommendation and reasons before collaborative convergence. Architecture links to that comparison method. Follow-up questions should distinguish the approaches and respond to the user's answers. Explicitly delegated choices remain the lead's to decide and explain without an approval stop; constraint-determined choices need no invented alternatives. The charter records the clarified experience.

Selective consultation, proportionate small-task handling, ordinary implementation freedom, read-before-dependent-action, existing runbook notices, Arena confirmation and request-only reflection are retained. There is no new skill, runtime, mandatory menu or workflow stage. Package and marketplace versions advance together to 0.7.5 as a correction to existing behavior.

## Native observations

The preselected comparison used the same native Codex host, Sol at low reasoning, raw tasks, fixtures and evaluator for main and candidate. Explicit project-local Atlas activation preceded ordinary follow-ups; this does not test automatic skill discovery or a workplace Copilot installation.

| Coverage | Observations |
| --- | --- |
| Unprompted visible alternatives across two design turns | Main and final candidate read discovery and presented alternatives and recommendations. Both responded to a fairness/workload amendment. The candidate made a strongest counterargument explicit, but both still elaborated substantial policy bundles before the user selected a direction. These samples do not establish improved conversational reliability. |
| Reported slowdown without measurements | Main used behavior tracing; final candidate read the specialist index and Performance. Both identified the nested history scan as a hypothesis and proposed measurement without claiming measured causation or changing files. The more specific selection is observed; superior advice is not established. |
| Prospective state-model change without an observed defect | Main used architecture; final candidate read Types and invariants. Both examined construction/import compatibility and state-dependent data while leaving missing policy unresolved. Broader relevant guidance is not automatically a routing failure. |
| Small authorized repair | Main and the earlier candidate corrected the boundary, ran a separating regression check and preserved neighboring behavior without an architecture exercise or new approval. This candidate predates the final delegated-judgment clarification. |
| Explicitly delegated planning | Earlier and final candidates created a proposal with reasons and unresolved evidence without another approval stop or implementation. Artifact creation is not proof of implementation readiness or complete design correctness. |
| Mechanical and constraint-determined controls | The earlier candidate returned only the sentence correction. The final candidate supplied the requested Markdown checkbox example without a design menu or file changes. |

Ten native samples completed across main, the earlier candidate and final candidate. This is a coverage count, not a pass rate. An additional candidate case stopped during evaluator preflight because the source worktree's Git metadata could not be read, before a native task began. The remaining cases ran from a self-contained checkout with the same final guidance; that failed setup remains retained. The delegated control was deliberately repeated after its wording clarification. Completed samples recorded host teardown and removal of temporary authentication homes.

The two collaborative samples changed no planning files. They demonstrated visible conversation, but did not establish the continuing brief required for unresolved work; the candidate also announced an unnecessary no-artifact constraint. Their substantial policy proposals and missing continuity remain limitations rather than evidence of fully resolved discovery. The prewritten second turn is not a responsive human interview, and alternatives must still be judged for viability against the user's actual goals rather than counted.

## Independent review and objective checks

A first independent source review timed out after emitting a partial concern about delegated judgment at the core/charter level. That ambiguity was clarified, including the discovery question's ownership condition. Fresh independent source review of the corrected diff found no concrete defect; it did not execute the guidance or establish model adherence.

A separate independent reader assessed the native responses, tool excerpts and artifacts with condition labels that did not identify the candidate. It found useful comparisons in both conditions, a more explicit reservation/queue tradeoff and cultural counterargument in the candidate, and valid diagnostic advice in both. It also identified unsupported fairness rankings and policy detail requiring validation; no general quality or reliability improvement was established. The lead corroborated successful guide reads and writes against complete raw records where the review packet clipped outputs. The delegated proposal exercises delegated judgment, not user acceptance or verified operational suitability.

The 39 evaluator tests passed. Relative file targets in the core, charter and reference library resolved; aligned package versions and whitespace checks passed. The new cases use existing objective checks plus semantic review criteria rather than validators of exact phrases, headings or option counts. These infrastructure and source checks do not prove the requested experience.

## Limits

This correction makes the intended behavior more explicit and demonstrates relevant use in bounded synthetic sessions. It does not establish reliable triggering across all references, long conversations, compaction, other models or workplace hosts. No failing workplace-session trace was available, so unavailable guidance, host loading, selection and poor application remain distinct possible causes. Successful main samples limit improvement claims. Raw prompts, candidate identities, all attempts and tool evidence remain local maintainer evidence.
