# Retained synthetic replay input

`garden-prd.md` is the flawed PRD generated by the initial transition trial. It is deliberately frozen, including the broader volunteer-visibility inference under agreed requirements. Only its unavailable predecessor pointer was replaced for standalone replay use.

Do not treat it as an example of correct Atlas output or edit away a recorded defect to improve a completed trial. `transition-plan-replay` supplies it as input to downstream ticket/handoff planning; it does not exercise PRD-generation attribution. The full `transition-chain` case exercises that correction against its user-supplied decisions. This note is maintainer context and is not injected into the fixture.
